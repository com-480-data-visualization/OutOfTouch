# Use a lightweight web server as the base image
FROM nginx:alpine

# Copy the HTML and JavaScript files to the web server directory
COPY index.html /usr/share/nginx/html
COPY main.js /usr/share/nginx/html